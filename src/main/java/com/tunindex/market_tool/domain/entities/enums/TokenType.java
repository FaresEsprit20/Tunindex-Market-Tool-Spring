package com.tunindex.market_tool.domain.entities.enums;

public enum TokenType {
    JWT,           // For authentication tokens
    PASSWORD_RESET, // For password reset tokens
    TWO_FACTOR     // For 2FA tokens
} 